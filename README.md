# Environment-Aware-Context-Protocol
The Environment-Aware Context Protocol (EACP) is a framework-independent open protocol that enables AI coding agents to "understand" a user's specific local execution environment and generate code optimized for that environment.
